// Back-compat shim. The canonical helper lives in `@/lib/entityTerminology`.
// New code should import from there (or use the `useEntityText` hook).
export { applyEntityTerminology } from '@/lib/entityTerminology';
