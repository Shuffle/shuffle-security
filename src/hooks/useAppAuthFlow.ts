/**
 * useAppAuthFlow — re-exported from the Shuffle-MCPs library.
 *
 * The canonical implementation now lives in `src/Shuffle-MCPs/useAppAuthFlow.ts`
 * so the library's AddAppModal can use it without host-app imports.
 */

export { useAppAuthFlow } from '@/Shuffle-MCPs/useAppAuthFlow';
