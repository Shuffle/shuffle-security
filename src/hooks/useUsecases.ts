/**
 * Re-export shim — the canonical Usecases hook now lives in Shuffle-Core
 * (`@/Shuffle-Core/hooks/useUsecases`). This file is kept so existing host
 * imports (`@/hooks/useUsecases`) keep resolving.
 */
export * from '@/Shuffle-Core/hooks/useUsecases';
