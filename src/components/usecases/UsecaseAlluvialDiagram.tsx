/**
 * Re-export shim — the canonical Usecase alluvial diagram now lives in
 * Shuffle-Core (`@/Shuffle-Core/views/UsecaseAlluvialDiagram`). Kept here so
 * `@/components/usecases/UsecaseAlluvialDiagram` imports keep resolving.
 */
export { default } from '@/Shuffle-Core/views/UsecaseAlluvialDiagram';
