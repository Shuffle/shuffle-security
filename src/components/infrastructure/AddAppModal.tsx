/**
 * AddAppModal — re-exported from the Shuffle-MCPs library.
 *
 * The canonical implementation now lives in `src/Shuffle-MCPs/components/AddAppModal.tsx`
 * so the library's AppSearchDrawer can use the same modal without host-app imports.
 */

export { AddAppModal, default } from '@/Shuffle-MCPs/components/AddAppModal';
export type { AddAppModalProps } from '@/Shuffle-MCPs/components/AddAppModal';
