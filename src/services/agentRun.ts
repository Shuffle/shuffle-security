/**
 * Re-export shim — the canonical implementation lives in the standalone
 * Shuffle-MCPs library. Keep this file so existing host-app imports of
 * `@/services/agentRun` continue to work.
 */
export * from '@/Shuffle-MCPs/agentRun';
