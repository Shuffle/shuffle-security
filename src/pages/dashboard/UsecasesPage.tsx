/**
 * UsecasesPage — thin host wrapper around the standalone implementation that
 * now lives in `@/Shuffle-Core`. Keep this file as a re-export so the existing
 * route (`/usecases` etc.) keeps working.
 */
export { default } from '@/Shuffle-Core/Usecases';
